[build]
  # Build command will be set up via build script or manual build
  # For now, build locally: flutter build web
  publish = "build/web"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

